# Manifest Schema

Each registered project must have a manifest file in the `manifests/` folder.

## Example

```json
{
  "name": "example-project",
  "baseUrl": "https://example.com",
  "testDir": "./playground"
}
```
